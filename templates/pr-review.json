{
  "id": "pr-review",
  "name": "PR Review",
  "description": "Review a newly opened pull/merge request. Posts inline comments and a summary review.",
  "category": "review",
  "workflows": [
    {
      "name": "PR Review",
      "description": "Reads the pull/merge request diff, posts inline comments where appropriate, leaves a summary review.",
      "definition": {
        "triggers": [
          {
            "id": "trigger-pr-review",
            "name": "PRReviewTrigger",
            "platform": "github",
            "connectionId": "<github-repo>",
            "type": "pull_requests",
            "intervalSec": 60,
            "filters": []
          }
        ],
        "nodes": [
          {
            "id": "agent-pr-review",
            "name": "Review",
            "presetId": "pr-reviewer",
            "instructionsAppend": "A pull/merge request has just been opened. Read its metadata from the trigger context. Submit your review with COMMENT state only via the connected MCP server. Read local guidance such as CLAUDE.md or CONTRIBUTING.md when present.",
            "mcpServers": [{ "serverId": "github-mcp" }],
            "skills": []          }
        ],
        "edges": [{ "from": "PRReviewTrigger", "to": "Review" }],
        "mcpServers": [
          {
            "id": "github-mcp",
            "presetId": "github",
            "connectionId": "<github-repo>"
          }
        ],
        "ui": {
          "nodePositions": {
            "PRReviewTrigger": { "x": 80, "y": 120 },
            "Review": { "x": 440, "y": 120 }
          },
          "viewport": { "x": 0, "y": 0, "zoom": 1 }
        }
      }
    }
  ]
}
