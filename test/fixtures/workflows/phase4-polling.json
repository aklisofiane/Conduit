{
  "name": "Phase 4 polling — status=Dev triage",
  "description": "Polls a GitHub Projects v2 board on a long interval (schedule is triggered manually in the E2E test). One agent reacts per matching item.",
  "definition": {
    "triggers": [
      {
        "id": "trigger-phase4",
        "name": "Phase4Trigger",
        "platform": "github",
        "connectionId": "__placeholder_repo__",
        "boardConnectionId": "__placeholder_board__",
        "type": "issues",
        "intervalSec": 3600,
        "filters": [{ "field": "status", "value": "Dev" }]
      }
    ],
    "nodes": [
      {
        "id": "agent-dev",
        "name": "Dev",
        "provider": "claude",
        "model": "stub-model",
        "instructions": "Pick up the ticket entering the Dev column.",
        "mcpServers": [],
        "skills": []
      }
    ],
    "edges": [{ "from": "Phase4Trigger", "to": "Dev" }],
    "mcpServers": [],
    "ui": {
      "nodePositions": {
        "Phase4Trigger": { "x": 80, "y": 120 },
        "Dev": { "x": 440, "y": 120 }
      },
      "viewport": { "x": 0, "y": 0, "zoom": 1 }
    }
  }
}
